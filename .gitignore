# Xcode / XcodeGen
*.xcodeproj/
*.xcworkspace/
xcuserdata/
DerivedData/
.build/
.swiftpm/

# macOS
.DS_Store

# Backend — código versionado, mas segredos reais e dados do banco ficam de fora.
api/.env
api/database/*.sql

# Local tooling config (Claude Code permission cache, machine-specific)
.claude/

# Local signing config (Apple Developer Team ID) — copy Local.xcconfig.example
SabeALei/Config/Local.xcconfig
