# Xcode / XcodeGen
*.xcodeproj/
*.xcworkspace/
xcuserdata/
DerivedData/
.build/
.swiftpm/

# macOS
.DS_Store

# Backend — kept out of this repo entirely; only the iOS app is public here.
api/

# Local tooling config (Claude Code permission cache, machine-specific)
.claude/

# Local signing config (Apple Developer Team ID) — copy Local.xcconfig.example
SabeALei/Config/Local.xcconfig
