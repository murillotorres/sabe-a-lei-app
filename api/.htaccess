# Bloqueia acesso direto a tudo dentro de api/ (src/, database/, .env, etc.).
# Somente api/public/ deve ser exposta pelo servidor web.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
